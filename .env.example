# Copiar para .env.local (dev) ou definir na Vercel → Settings → Environment Variables
# Valores em: Supabase → Project Settings → API
#
# Recuperação de palavra-passe: em Supabase → Authentication → URL Configuration → Redirect URLs,
# adiciona (ajusta host e pasta se usares base path):
#   http://localhost:5173/redefinir-palavra-passe
#   https://<teu-dominio>/redefinir-palavra-passe

VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
